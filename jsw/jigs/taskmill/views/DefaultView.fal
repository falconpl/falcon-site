load core.View

/**
 * Task list view.
 *
 * @author 	Vladimir Florentino
 * @package Taskmill
 * @since 	0.1.0
 * @begin 	2009.4.13
 *
 * @param string jig - The name of the Jig that owns this view.
 */
class DefaultView( jig ) from View( jig )

	/**
	 * Constructor.
	 */
	init
		// Set the template this view will use.
		self.setTemplate('Shell', 'Default')
	end

	/**
	 * Process this view's logic.
	 *
	 * @return nil
	 */
	function process()
	end
end
