load core.Action

/**
 * Default application action.
 *
 * This action is executed when no action is explicitly given in the URL.
 * 
 * @author 	Vladimir Florentino
 * @package Taskmill
 * @since 	0.1.0
 * @begin 	2009.4.14
 */
class DefaultAction(jig) from Action(jig)
	/**
	 * Constructor.
	 */
	init
		self.setView('DefaultView')
 	end
end
